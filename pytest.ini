[pytest]

# File warnings to ignore markers warning

filterwarnings =
    error
    ignore::UserWarning
    ignore:function ham\(\) is deprecated:DeprecationWarning


markers =
    smoke = smoke scenarios
    homepage = homepage scenarios